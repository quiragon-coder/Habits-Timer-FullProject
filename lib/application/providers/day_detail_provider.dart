class DayDetailProvider {
  // Ici tu implémenteras deleteSessionCascade et la logique de récupération des sessions avec pauses
  Future<void> deleteSessionCascade(int sessionId) async {
    // TODO: supprimer session + pauses associées
  }
}